# ⚠️ ВАЖНО: Проверка настройки системы отзывов

## Проблема: Ошибка "Error inserting review"

Если вы видите ошибку при отправке отзыва, скорее всего **SQL миграции не были выполнены**.

---

## ✅ Шаг 1: Проверьте, созданы ли таблицы

Откройте **Supabase Dashboard → SQL Editor** и выполните:

```sql
-- Проверка таблицы reviews
SELECT EXISTS (
   SELECT FROM information_schema.tables 
   WHERE table_schema = 'public'
   AND table_name = 'reviews'
);
```

**Если результат `false`** - таблица НЕ создана, нужно запустить миграции!

```sql
-- Проверка поля review_modal_shown
SELECT EXISTS (
   SELECT FROM information_schema.columns 
   WHERE table_schema = 'public'
   AND table_name = 'user_credits'
   AND column_name = 'review_modal_shown'
);
```

**Если результат `false`** - поле НЕ добавлено!

```sql
-- Проверка RPC функции
SELECT EXISTS (
   SELECT FROM pg_proc 
   WHERE proname = 'award_review_bonus'
);
```

**Если результат `false`** - RPC функция НЕ создана!

---

## ✅ Шаг 2: Запустите SQL миграции

### Миграция 1: Создание таблицы reviews

Откройте файл:
```
supabase/migrations/20250104_create_reviews_table.sql
```

**Скопируйте ВЕСЬ код из файла** и вставьте в **Supabase Dashboard → SQL Editor** → нажмите **RUN**.

Это создаст:
- ✅ Таблицу `reviews`
- ✅ RLS policies
- ✅ Индексы
- ✅ Поле `review_modal_shown` в таблице `user_credits`

### Миграция 2: Создание RPC функции

Откройте файл:
```
supabase/migrations/20250104_award_review_bonus_function.sql
```

**Скопируйте ВЕСЬ код из файла** и вставьте в **Supabase Dashboard → SQL Editor** → нажмите **RUN**.

Это создаст:
- ✅ RPC функцию `award_review_bonus(user_id, email)`

---

## ✅ Шаг 3: Проверьте Secrets в Edge Function

Откройте: **Supabase Dashboard → Functions → submit-review → Settings → Secrets**

Убедитесь, что добавлены:

```
VITE_N8N_WEBHOOK_URL_REVIEWS = (ваш URL из .env)
VITE_N8N_SECRET_TOKEN_REVIEWS = (ваш токен из .env)
```

Если их нет - добавьте!

---

## ✅ Шаг 4: Протестируйте систему

### Тест 1: Проверить таблицу
```sql
SELECT * FROM reviews LIMIT 1;
```

Должно вернуть пустой результат (0 rows) - это нормально, значит таблица создана!

### Тест 2: Проверить RPC функцию
```sql
-- Замените на свой user_id и email
SELECT award_review_bonus(
  'ваш-user-id'::UUID, 
  'your-email@example.com'
);
```

Должно вернуть JSON с `"success": false, "reason": "bonus_already_received"` или похожее.

### Тест 3: Попробуйте оставить отзыв через сайт

1. Откройте `http://localhost:8080/review`
2. Выберите рейтинг (1-5 звезд)
3. Напишите отзыв (минимум 20 символов)
4. Нажмите **Submit Review** (желтая кнопка)

**Если всё настроено правильно:**
- ✅ Отзыв сохранится
- ✅ Начислится +3 кредита
- ✅ Появится success сообщение

---

## 🐛 Если все еще ошибка

### Проверьте логи Edge Function

**Supabase Dashboard → Functions → submit-review → Logs**

Найдите последнюю ERROR запись и посмотрите детали.

### Типичные ошибки:

1. **"relation 'reviews' does not exist"**
   - Миграция 1 не выполнена
   - Решение: Запустите SQL из `20250104_create_reviews_table.sql`

2. **"function award_review_bonus does not exist"**
   - Миграция 2 не выполнена
   - Решение: Запустите SQL из `20250104_award_review_bonus_function.sql`

3. **"duplicate key value violates unique constraint"**
   - Пользователь уже оставил отзыв
   - Решение: Это нормально, один пользователь может оставить только один отзыв

4. **"new row violates row-level security policy"**
   - Проблема с RLS policies
   - Решение: Убедитесь что миграция 1 выполнена полностью

---

## 📊 Проверка работы модального окна

### Условия показа модала:

1. ✅ Пользователь авторизован
2. ✅ У пользователя ровно **1** свободный кредит (`free_credits = 1`)
3. ✅ Поле `review_modal_shown = FALSE` в таблице `user_credits`
4. ✅ Пользователь еще НЕ оставлял отзыв

### Как протестировать:

```sql
-- Установить у себя 1 кредит (замените на свой user_id)
UPDATE user_credits 
SET free_credits = 1, 
    paid_credits = 0,
    review_modal_shown = FALSE
WHERE user_id = 'ваш-user-id';
```

Теперь:
1. Перейдите на главную страницу `http://localhost:8080`
2. Подождите 2 секунды
3. Должно появиться модальное окно с предложением оставить отзыв

---

## ✅ Checklist развертывания

- [ ] Выполнена миграция 1 (`20250104_create_reviews_table.sql`)
- [ ] Выполнена миграция 2 (`20250104_award_review_bonus_function.sql`)
- [ ] Добавлены Secrets в Edge Function
- [ ] Протестирована отправка отзыва
- [ ] Проверен показ модального окна

---

## 🆘 Нужна помощь?

Если ошибка продолжается:
1. Сделайте скриншот логов из **Functions → submit-review → Logs**
2. Скопируйте текст ошибки
3. Проверьте, что все миграции выполнены (см. Шаг 1)
